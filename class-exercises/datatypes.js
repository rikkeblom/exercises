"use strict";
console.log("Hello World");
const bool = true;
const num = 41;
const str = "Peter";
const obj = {
  cats: 2,
  cars: 1,
};
const nothing = null;
let undf;
const symbol = Symbol("symbol");

//find the value of the constant or variable in the live-server console by typing the name and clicking enter
//use "typeof" followed by the name in the console to see the datatype
